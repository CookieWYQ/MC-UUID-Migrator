# v1.0.0 — 首个正式版本

> MC UUID 玩家数据迁移工具的第一个公开版本：把「玩家 A 的所有数据」整体迁移成「玩家 B 的」，并新增 Xaero 路径点 / 已探索地图的容器间转移。

## 下载

| 文件               | 大小       | SHA256                                                             |
| ---------------- | -------- | ------------------------------------------------------------------ |
| `UUID玩家数据迁移.exe` | 17.16 MB | `7A9B360359B4E0AD4332542D1393F6A12449169D0F95DBA03DC6E751213480AC` |

- Windows 64 位，绿色**单文件**：无需安装 Java / Python，双击即用

- 依赖（paramiko / cryptography / bcrypt / tkinter）与程序图标均已打包进 exe

- 内置 10 篇图形教程，点击界面右上角「使用教程」即可查看

## 本版功能

### UUID 玩家数据迁移

- 文件名 / 目录名改名：`playerdata`、`stats`、`advancements`、`maid_backups` 等

- 内容原位替换：纯文本、二进制、gzip 压缩 NBT、`.mca` 区块（逐区块解压重建）

- 覆盖 UUID 的 4 种写法（大小写 × 带横线/无横线）与 16 字节二进制形式

- 不依赖固定路径与版本号，适用于所有以 UUID 作玩家标识的版本

### 单人存档模式

- 纯本地操作，全程离线

### 多人服务器模式（SFTP）

- 远程迁移服务器世界：列出文件 → 下载镜像 → 本地替换 → 只回传改动文件

- 单通道复用（避免部分主机重复开通道卡死）、逐操作超时、扫描 180s 硬超时看门狗、列目录实时进度

- 连接测试按钮；密码可留空；服务器信息自动保存（密码可选记住，混淆存储）

### Xaero 地图转移（新）

- **路径点**：按维度合并到目标容器，自动去重

- **已探索地图**：只补充缺失区块，不覆盖目标已有数据

- **多世界 ID 对齐**：读取服务器 `world/xaeromap.txt` 的 `id:<数字>`，自动以 `mw$<ID>_1.txt` 命名；服务器换 ID 时可一键改名对齐

- 维度自动映射：`dim%0 ↔ null`、`dim%-1 ↔ DIM-1`、`dim%1 ↔ DIM1`

- 说明：Xaero 路径点属于**客户端 + 容器**，与账号 UUID 无关

### 其他

- 一键替换：输入用户名 + 方向（正版→离线 / 离线→正版），自动算出两个 UUID 并执行

- 迁移前自动备份，可整体还原（详见 README「备份与还原」）

- 图形界面 + 完整命令行参数（`--help` 查看）

## 使用提示

1. 迁移服务器世界前**先停服**，使用 Xaero 转移前**先退出游戏**
2. 建议先点「扫描 / 扫描预览」确认命中范围，再执行迁移
3. 任何一步出问题都可用自动生成的备份目录还原

## 已知限制

- 若模组以 NBT 的 `UUIDMost / UUIDLeast` 双 long（而非字符串或 16 字节数组）形式存 UUID，字节级替换无法命中；但玩家本体数据（背包 / 位置 / 成就等）依靠文件名改名继承，不受影响

- 多人模式的 SFTP 需主机面板开放该通道

## 致谢与声明

- 作者：**CallMeACookieWYQ**，许可：[MIT](https://github.com/CookieWYQ/MC-UUID-Migrator/blob/main/LICENSE)

- 本 Release **不含任何第三方模组文件**；提到的 Xaero 等名称仅用于说明兼容性，版权归原作者所有

- 请遵守所在服务器规则与相关法律法规使用本工具

